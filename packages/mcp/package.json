{
  "name": "@astrograph/mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "MCP server over the official @modelcontextprotocol/sdk — Stage 2 (see docs/tools.md). SDK + transport deps added when implemented.",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@astrograph/core": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
